import * as ping from "./ping";
import * as setChannel from "./setChannel";
import * as quoteMe from "./quoteMe";
import * as quoteMuch from "./quoteMuch";

export const commands = {
    ping,
    "set-channel": setChannel,
    "quote-me": quoteMe,
    "quote-much": quoteMuch,
};
